{
  "name": "team-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Shared team expense tracker - Express + SQLite + React",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "reset": "npm run reset --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
